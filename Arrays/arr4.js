// search for a value in Array 
var names = ["David", "Cynthia", "Raymond", "Clayton", "Jennifer"];
var target = 'Raymond';
if(names.indexOf(target)!=-1){
    console.log(`${target} found in names`);
}